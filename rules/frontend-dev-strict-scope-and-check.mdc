## 前端开发强约束

> 本文件所有规则均为**强制**执行；如出现“建议/倾向/尽量/优先”等措辞，统一按**必须**理解。
> 当提示词和rule冲突时，优先原则：用户输入的提示词>下面所有原则

### 适用场景
- React、Vue、Umi 等 **前端项目开发/改动代码** 场景

## PRD 范围与落地（强制）
- **PRD 声明的功能模块与验收点必须全部实现**：以用户提供的 PRD / 需求整理文档（含 html、带图附件）为交付清单，**逐条覆盖**；禁止因“估时、优先级、难易、分 PR”等理由**在未获用户明确书面同意**的前提下自行决定只实现其中一块。
- **允许删减的唯一条件**：用户在同一轮对话/同一需求单中**明文写出**“本期不做 XXX”或等价表述；否则默认**全文落地**。
- **图片/示意与文案**：PRD 中的截图用于确认**布局、换行、控件相对位置、选项顺序**；实现须与图一致；若图与文字冲突，以 PRD **当期**说明为准并在回复中点明取舍依据。

## 限制（强制禁止）
- 禁止在用户**未提供或未引用**具体需求材料的情况下，凭空增加 PRD 之外的功能
- 禁止用“先上了一部分”替代 PRD 全量交付却不向用户说明剩余项与排期
- 禁止除用户提供的明确需求（包含用户提供的html文件）以外，自动实现任何需求
- 禁止对没有引用的代码进行修改
案例：
components/Bread.js仅被一处引用，但是实际上注释了
// import BreadcrumbCustom from 'components/Bread';
因此components/Bread.js这个文件没有被使用，因此禁止对这个文件进行任何修改

## 检查（每次 AI 代码改动后必须执行）
- 没改动到的 if 分支、没改动到的原有功能，是否有任何改动（例如换行等）；如果有，必须改回原有代码，而不是“保证功能不变”即可
- 当某个功能块改了，之后又让撤回去不改了，必须保证跟原来一模一样（换行、逗号等一个符号都不能变）
- 每一处改动必须能指向需求原文的一句话；如果不能，则禁止实现
- 表单的里面 radio 的顺序，必须跟图片保持一致
- 如果 prd 里的图片有两行，则必须文字跟 prd 的文案保持一致，但是换行要根据图片来换行
- placeholder 跟图片里保持完整一致，如果 prd 文案出现 placeholder，则按照 prd 的文案为准
- 左右布局或者上下布局跟图片保持一致，例如 radio（文案+时间选择器），其中图片是文案在左时间选择器在右，表单也是这样，label 在左或者在上，根据图片来确认，默认就用左右，很少 form 是上下结构
